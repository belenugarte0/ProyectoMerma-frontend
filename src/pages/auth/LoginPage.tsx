import { LoginForm } from "../../components"

export const LoginPage = () => {
    return (
        <section className="container">
            {/* FORMULARIO */}
            <LoginForm/>

        </section>
    )
}
